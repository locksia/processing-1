2.키보드로 속도 조절이 가능한 배너

void setup() {
size(800, 300);
textSize(50);
}
int i,a=1;
void draw() {
fill(0);
background(255,255,0);
text("andong",i=i+a,150);<---배너 생성,가로 위치가 계속 증가
if(i>800){<---끝까지 가면 다시 처음부터 시작
    i=0;
   }
}
void keyPressed(){<---키보드에서 숫자 입력
a = key-'0';
}
